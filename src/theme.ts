import { createTheme } from '@mui/material';

export const theme = createTheme({
	palette: {
		primary: {
			main: '#34374c',
			dark: '#2c2e3e',
		},
		secondary: {
			main: '#ffffff',
			dark: '#f6f6f6',
		},
	},
});
